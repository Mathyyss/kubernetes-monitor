quand j'appelle api/pods il execute 'kubectl get pods -A -o json'

Il lit la sortie et renvoie en JSON 

Lancer le serveur 

```
node server.js
```

Ouvrir le serveur 
```
curl http://localhost:5000
```
